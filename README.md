# INSTRUCTIONS:

```bash
mkdir -p ~/projects
git clone ssh://git@github.com/khanduri/bitstarter ~/projects/bitstarter
cd ~/projects/bitstarter
npm install
```

The ```npm install``` will read the dependencies defined in package.json 
and install them
